import { PageSkeleton } from "@/components/ui/skeleton";

/**
 * Loading state for the home page
 * Displays while the page content is being fetched
 */
export default function Loading() {
  return <PageSkeleton />;
}
